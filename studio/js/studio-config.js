/**
 * Optional studio settings (copy from studio-config.example.js if needed).
 * muxApiBase: where Netlify functions run when using Live Server on port 5500.
 */
window.BurnfolderStudioConfig = window.BurnfolderStudioConfig || {
  // Example when studio is on Live Server but functions run via `netlify dev`:
  // muxApiBase: 'http://localhost:8888/.netlify/functions'
};
